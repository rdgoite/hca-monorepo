rootProject.name = 'hca-monorepo'

def ingestModuleNames = ['accessioner', 'broker', 'core', 'exporter', 'kube-deployment',
        'staging-manager', 'validator']
ingestModuleNames.each { name ->
    include "ingest-$name"
}